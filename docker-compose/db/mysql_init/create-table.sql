CREATE database app_db IF NOT EXISTS `app_db`;

USE app_db;

-- DROP TABLE IF EXISTS `Like`;

-- CREATE TABLE `Like`
--     (user_id INTEGER UNSIGNED,
--     post_id INTEGER UNSIGNED
--     );

-- INSERT
--     INTO `Like`(user_id, post_id)
--     VALUES 
--         (1, 2),
--         (2, 3),
--         (1, 3);